---
description: 
globs: 
alwaysApply: true
---

# 在你一个需求修改最终完成后，进行下面的操作

1. `nr lint:fix && nr build`，没有错误之后下一步
2. `git add . && git commit -m <适合的 code message> && git push`
3. `nr bump <patch/minor/major，选择合适的>`
4. 在`zsh`里面运行`nr submit:latest`
